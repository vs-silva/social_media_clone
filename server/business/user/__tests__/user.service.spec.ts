import {describe, it} from "vitest";

describe('User service tests', () => {

    describe('RegisterUser port tests', () => {

        it.todo('RegisterUser port should return null if required fields of RequestUserRegisterDTO are not provided');
        it.todo('RegisterUser port should create a new user and ResponseUserDTO');

    });

});
